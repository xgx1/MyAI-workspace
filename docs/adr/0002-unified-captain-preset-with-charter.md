# 统一「队长模式」预设 + 类别章程注入

队长分类是动态的，而预设是静态目录——为每个类别铸造专属预设会随类别无限膨胀。决定只做一个从全能模式扩展的「队长模式」预设，类别差异全部由派发时注入的类别章程（数据层）承载，预设层不做类别分化。

## Considered Options

- Manager 运行时为新类别铸造专属预设（copy omni）：被否——每类一个预设目录、每次生成都要 mount 校验，用膨胀和脆弱换取的只是章程本可承载的差异。
- 预置固定类别库（研究/实现/评审…）：被否——等于放弃动态分类，与需求相悖。
