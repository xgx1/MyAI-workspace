# 子仓（deepseek-harness / dsh-extensions）**不再是忽略项**——它们已登记为
# git submodule，由 .gitmodules 记录（见 docs/adr/0005）。本仓保存指向各自提交的指针。
#
# 注：update-app 位于 ~/projects/update-app（工作区之外），因此不是本仓的子模块。

# 运行时状态（AgentTeams 的任务/成员/邮箱状态，可再生）
/.agent-teams/

# 通用
node_modules/
.playwright-mcp/
*.tar.gz

# zvec-grep 的工作区索引（本机产物，可再生；删了用 `zg index --rebuild` 重建）
.zvec-grep/
